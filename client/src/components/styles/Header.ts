import styled from "styled-components";

const Header = styled.div`
    display: flex;
    flex-direction: row;
    .date {
        margin-top: 20px;
        margin-left: 20px;
    }
`;

export default Header;